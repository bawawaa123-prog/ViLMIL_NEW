# Historical Code

This directory contains older implementations kept for reference. Active training and evaluation use the modules in the project root and the package directories (`datasets/`, `models/`, `utils/`, and `wsi_core/`).
